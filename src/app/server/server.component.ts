import { Component } from '@angular/core';

@Component({ // decorator
    selector: 'app-server',
    templateUrl: './server.component.html',
    styles: [`
        p {
            color: dodgerblue;
        }
    `]
})

export class ServerComponent {

}